<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wait.mapper.UserBlockMapper">

    <insert id="insert" parameterType="com.wait.entity.domain.UserBlock" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_block (user_id, blocked_user_id, created_at)
        VALUES (#{userId}, #{blockedUserId}, #{createdAt})
    </insert>

    <delete id="delete">
        DELETE FROM user_block WHERE user_id = #{userId} AND blocked_user_id = #{blockedUserId}
    </delete>

    <select id="exists" resultType="boolean">
        SELECT COUNT(*) > 0 FROM user_block WHERE user_id = #{userId} AND blocked_user_id = #{blockedUserId}
    </select>

    <select id="selectBlockedUserIds" resultType="java.lang.Long">
        SELECT blocked_user_id FROM user_block WHERE user_id = #{userId}
    </select>

    <select id="batchCheckBlocked" resultType="java.lang.Long">
        SELECT blocked_user_id FROM user_block
        WHERE user_id = #{userId} AND blocked_user_id IN
        <foreach collection="targetUserIds" item="targetUserId" open="(" separator="," close=")">
            #{targetUserId}
        </foreach>
    </select>

</mapper>

