<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wait.mapper.PostFavoriteMapper">

    <insert id="insert">
        INSERT INTO post_favorite (user_id, post_id, created_at) 
        VALUES (#{userId}, #{postId}, #{createdAt})
    </insert>

    <delete id="delete">
        DELETE FROM post_favorite WHERE user_id = #{userId} AND post_id = #{postId}
    </delete>

    <!-- 批量删除 -->
    <delete id="batchDelete">
        DELETE FROM post_favorite WHERE (user_id, post_id) IN
        <foreach collection="deletes" item="item" open="(" separator="," close=")">
            (#{item.userId}, #{item.postId})
        </foreach>
    </delete>

    <select id="exists" resultType="boolean">
        SELECT COUNT(*) > 0 FROM post_favorite WHERE user_id = #{userId} AND post_id = #{postId}
    </select>

    <select id="countByPostId" resultType="int">
        SELECT COUNT(*) FROM post_favorite WHERE post_id = #{postId}
    </select>

    <select id="selectPostIdsByUserId" resultType="java.lang.Long">
        SELECT post_id FROM post_favorite WHERE user_id = #{userId}
    </select>

    <!-- 批量查询存在性：返回已存在的 (userId, postId) 对 -->
    <select id="batchExists" resultType="com.wait.entity.domain.PostFavorite">
        SELECT user_id AS userId, post_id AS postId
        FROM post_favorite
        WHERE (user_id, post_id) IN
        <foreach collection="favorites" item="item" open="(" separator="," close=")">
            (#{item.userId}, #{item.postId})
        </foreach>
    </select>

    <insert id="batchInsert">
        INSERT INTO post_favorite (user_id, post_id, created_at) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.postId}, #{item.createdAt})
        </foreach>
    </insert>

    <select id="selectAll" resultType="com.wait.entity.domain.PostFavorite">
        SELECT user_id AS userId, post_id AS postId, created_at AS createdAt 
        FROM post_favorite
    </select>

    <!-- 查询所有有收藏的帖子ID和收藏数（用于定时同步） -->
    <select id="selectAllPostFavoriteCounts" resultType="com.wait.entity.domain.PostFavorite">
        SELECT post_id AS postId, COUNT(*) AS id
        FROM post_favorite
        GROUP BY post_id
    </select>

    <!-- 查询所有有收藏的用户ID列表（去重） -->
    <select id="selectDistinctUserIds" resultType="java.lang.Long">
        SELECT DISTINCT user_id FROM post_favorite ORDER BY user_id
    </select>

    <!-- 分页查询有收藏的用户ID列表 -->
    <select id="selectDistinctUserIdsWithPaging" resultType="java.lang.Long">
        SELECT DISTINCT user_id FROM post_favorite ORDER BY user_id LIMIT #{limit} OFFSET #{offset}
    </select>

</mapper>

